/**
 * Author: Fellipe Lauton C. Pinto.
 * Description: this is the data transition model needed to perform Pass Control.
 */

export interface PassControlValidation {
  conversationId: string;
  integrationId: string;
  first_message_id: string;
}
